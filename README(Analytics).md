2 Задание: Работа со строками. 
4 Вариант: Реализуйте рекурсивную функцию для переворота строки .
Контрольный вопрос 7: Какова временная сложность задачи о N ферзях?
📝 Определение

Рекурсивная функция для переворота строки - это функция, которая вызывает саму себя для решения задачи обращения порядка символов в строке путем разбиения ее на более мелкие подзадачи до достижения базового случая.

Формальное определение:
reverse_string(s) = 
    если |s| ≤ 1: s                    (базовый случай)
    иначе: s[-1] + reverse_string(s[0:-1])  (рекурсивный случай)

---

🔍 Анализ работы алгоритма

Принцип работы базовой реализации:

Рекурсивная стратегия:
reverse_string("hello") = 
    'o' + reverse_string("hell")
        'l' + reverse_string("hel")
            'l' + reverse_string("he")
                'e' + reverse_string("h")
                    'h' (базовый случай)
                = 'e' + 'h' = "eh"
            = 'l' + "eh" = "leh"
        = 'l' + "leh" = "lleh"
    = 'o' + "lleh" = "olleh"

Принцип работы эффективной реализации:

· Использует технику двух указателей
· Меняет симметричные символы местами
· Рекурсивно обрабатывает внутреннюю часть строки

---
Временная сложность для трех языков

📊 Сравнительный анализ временной сложности

1. Java - Временная сложность

Базовая реализация: O(n²)
public static String reverseString(String str) {
    if (str == null || str.length() <= 1) {  // O(1)
        return str;
    }
    
    char lastChar = str.charAt(str.length() - 1);        // O(1)
    String remaining = str.substring(0, str.length() - 1); // O(n) - КРИТИЧЕСКАЯ ОПЕРАЦИЯ!
    
    return lastChar + reverseString(remaining);          // T(n-1) + O(n)
}

Операционный анализ Java:

· str.charAt(): O(1)
· str.substring(): O(n) - создает новую строку
· Конкатенация строк: O(n) - создает новую строку
· Рекурсивный вызов: T(n-1)

Рекуррентное соотношение:
T(n) = T(n-1) + O(n) + O(n) = T(n-1) + O(n)
T(1) = O(1)

Решение: T(n) = O(n²)

Эффективная реализация: O(n)
public static String reverseStringEfficient(char[] chars, int left, int right) {
    if (left >= right) {                    // O(1)
        return new String(chars);           // O(n) - только в конце
    }
    
    char temp = chars[left];               // O(1)
    chars[left] = chars[right];            // O(1)
    chars[right] = temp;                   // O(1)
    
    return reverseStringEfficient(chars, left + 1, right - 1); // T(n-2)
}

---

2. C++ - Временная сложность

Базовая реализация: O(n²)
string reverseString(const string& str) {
    if (str.length() <= 1) {               // O(1)
        return str;
    }
    
    char lastChar = str[str.length() - 1]; // O(1)
    string remaining = str.substr(0, str.length() - 1); // O(n) - КРИТИЧЕСКАЯ ОПЕРАЦИЯ!
    
    return lastChar + reverseString(remaining); // T(n-1) + O(n)
}

Операционный анализ C++:

· str[index]: O(1)
· str.substr(): O(n) - копирование подстроки
· Конкатенация: O(n) - создание новой строки
· Рекурсивный вызов: T(n-1)

Рекуррентное соотношение:
T(n) = T(n-1) + O(n) + O(n) = T(n-1) + O(n)
T(1) = O(1)

Решение: T(n) = O(n²)

Эффективная реализация: O(n)
void reverseStringEfficient(string& str, int left, int right) {
    if (left >= right) {                   // O(1)
        return;
    }
    
    swap(str[left], str[right]);           // O(1) - обмен по ссылке!
    reverseStringEfficient(str, left + 1, right - 1); // T(n-2)
}

---

3. Python - Временная сложность

Базовая реализация: O(n²)
def reverse_string(s):
    if len(s) <= 1:                       # O(1)
        return s
    
    last_char = s[-1]                     # O(1)
    remaining = s[:-1]                    # O(n) - КРИТИЧЕСКАЯ ОПЕРАЦИЯ!
    
    return last_char + reverse_string(remaining) # T(n-1) + O(n)

Операционный анализ Python:

· s[-1]: O(1)
· s[:-1]: O(n) - создание среза (копирование)
· Конкатенация: O(n) - создание новой строки
· Рекурсивный вызов: T(n-1)

Рекуррентное соотношение:
T(n) = T(n-1) + O(n) + O(n) = T(n-1) + O(n)
T(1) = O(1)

Решение: T(n) = O(n²)

Эффективная реализация: O(n)
def reverse_string_efficient(chars, left, right):
    if left >= right:                      # O(1)
        return
    
    chars[left], chars[right] = chars[right], chars[left] # O(1)
    reverse_string_efficient(chars, left + 1, right - 1) # T(n-2)

---

🎯 Сводная таблица временной сложности

Операция Java C++ Python
Базовая реализация O(n²) O(n²) O(n²)
Эффективная реализация O(n) O(n) O(n)
Доступ по индексу O(1) O(1) O(1)
Создание подстроки O(n) O(n) O(n)
Конкатенация строк O(n) O(n) O(n)
Обмен символов O(1) O(1) O(1)

---

🔍 Детальный анализ критических операций

Критическая операция №1: Создание подстроки
// Java - String.substring()
String remaining = str.substring(0, str.length() - 1); // O(n)
// C++ - string.substr()
string remaining = str.substr(0, str.length() - 1);    // O(n)
# Python - slicing
remaining = s[:-1]                                     // O(n)

Почему O(n): Все три языка создают новую строку при операции подстроки/среза, что требует копирования n символов.

Критическая операция №2: Конкатенация строк
// Java - конкатенация
return lastChar + reverseString(remaining);            // O(n)
// C++ - конкатенацияreturn lastChar + reverseString(remaining);            // O(n)
# Python - конкатенация
return last_char + reverse_string(remaining)           // O(n)

Почему O(n): Создается новая строка, содержащая все символы из обеих исходных строк.

---

📈 Эмпирическое подтверждение сложности

Тестовые данные для n = 5:
Базовая реализация:
Java: 15 операций
C++: 15 операций  
Python: 15 операций

Эффективная реализация:
Java: 3 операции
C++: 3 операции
Python: 3 операции

Формулы для расчета операций:

· Базовая: n(n+1)/2 операций
· Эффективная: ⌈n/2⌉ операций

---

💡 Ключевые выводы

1. Все три языка демонстрируют одинаковую асимптотическую сложность
2. Основной bottleneck - операции с подстроками и конкатенацией
3. Эффективная реализация достигает O(n) за счет работы с массивами символов
4. Различия между языками проявляются в константных множителях, но не в асимптотике

Рекомендации по оптимизации:

· Используйте массивы символов вместо строк для модификаций
· Избегайте создания новых строк в рекурсивных вызовах
· Используйте технику двух указателей для O(n) сложности
⏱️ Временная сложность

Базовая реализация: O(n²)

Подтверждение через операторы:
// В Java реализации:
char lastChar = str.charAt(str.length() - 1);        // O(1)
String remaining = str.substring(0, str.length() - 1); // O(n) - создание новой строки
return lastChar + reverseString(remaining);          // Рекурсивный вызов T(n-1)

Рекуррентное соотношение:
T(n) = T(n-1) + O(n)
T(1) = O(1)

Решение:
T(n) = T(n-1) + n
     = T(n-2) + (n-1) + n
     = T(n-3) + (n-2) + (n-1) + n
     = ...
     = 1 + 2 + 3 + ... + n
     = n(n+1)/2 ∈ O(n²)

Эффективная реализация: O(n)

Подтверждение через операторы:
// В Java эффективной реализации:
swap(chars[left], chars[right]);          // O(1)
reverseStringEfficient(chars, left+1, right-1); // T(n-2)

Рекуррентное соотношение:
T(n) = T(n-2) + O(1)
T(1) = O(1)
T(0) = O(1)

Решение:
T(n) = O(n/2) ≈ O(n)

---

📊 Подтверждение временной сложности

Эмпирические данные для n = 5 ("hello"):

Реализация Теоретическая сложность Фактическое количество операций
Базовая O(5²) = 15 15 операций
Эффективная O(5) = 3 3 операции

Рост количества операций:

n Базовая O(n²) Эффективная O(n)
5 15 3
10 55 6
20 210 11
50 1275 26
100 5050 51

---

🎓 Контрольный вопрос

Вопрос: Какова временная сложность задачи о N ферзях?

Ответ: Временная сложность задачи о N ферзях составляет O(N!)

Обоснование:

1. Алгоритмический анализ:
     def solve_n_queens(row, n, queens):
       if row == n:                    # Базовый случай
           save_solution()             # O(1)
           return
       
       for col in range(n):            # N вариантов - O(N)
           if is_safe(row, col):       # O(N) проверок
               place_queen(row, col)   # O(1)
               solve_n_queens(row + 1, n)  # Рекурсивный вызов T(N-1)
               remove_queen(row, col)  # O(1)
   2. Рекуррентное соотношение:
     T(N) = N × [O(N) + T(N-1)]
   T(0) = O(1)
   
   Решение:
   T(N) = O(N! × N) ≈ O(N!)
   3. Комбинаторная природа:
   · Для 1-й строки: N вариантов
   · Для 2-й строки: ≤ N-1 вариантов
   · Для 3-й строки: ≤ N-2 вариантов
   · ...
   · Всего: N × (N-1) × (N-2) × ... × 1 = N!

Практические значения:

· N=4: 4! = 24 проверок
· N=8: 8! = 40,320 проверок
· N=12: 12! ≈ 479 млн проверок
· N=15: 15! ≈ 1.3 трлн проверок
Эта сложность делает задачу о N ферзях классическим примером комбинаторного взрыва и демонстрирует ограничения полного перебора для больших значений N.
