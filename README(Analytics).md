Вариант 4. Контрольный вопрос 7
Задача: Реверс строки рекурсивным методом

📋 Оглавление

· Описание задачи
· Анализ алгоритма
· Временная сложность
· Запуск и использование
· Результаты
· Контрольный вопрос

Определение

Рекурсивная функция для переворота строки - это функция, которая вызывает саму себя для решения задачи обращения порядка символов в строке путем разбиения ее на более мелкие подзадачи до достижения базового случая.
Формальное определение:

reverse_string(s) = 
    если |s| ≤ 1: s                    (базовый случай)
    иначе: s[-1] + reverse_string(s[0:-1])  (рекурсивный случай)

🎯 Описание задачи

Вариант 4: Реализуйте рекурсивную функцию для переворота строки.

Задача: Написать рекурсивную функцию, которая принимает строку и возвращает её перевернутую версию.

Пример:
Вход: "hello"
Выход: "olleh"

Вход: "algorithm"
Выход: "mhtirogla"


🔍 Анализ алгоритма

Принцип работыОсновная идея: Разбить задачу на меньшие подзадачи до достижения базового случая.

Рекурсивная стратегия:
reverse_string(s) = 
    если s пустая или один символ: вернуть s
    иначе: последний_символ + reverse_string(строка_без_последнего_символа)

Детальный разбор для "hello":
reverse_string("hello") = 
    'o' + reverse_string("hell")
        'l' + reverse_string("hel")
            'l' + reverse_string("he")
                'e' + reverse_string("h")
                    'h' (базовый случай)
                = 'e' + 'h' = "eh"
            = 'l' + "eh" = "leh"
        = 'l' + "leh" = "lleh"
    = 'o' + "lleh" = "olleh"

Базовый случай:

· Пустая строка "" возвращает ""
· Строка из одного символа возвращает себя

Рекурсивный случай:

· Берем последний символ строки
· Рекурсивно переворачиваем оставшуюся часть
· Соединяем последний символ с перевернутой частью

⏱️ Временная сложность

Основная сложность: O(n²)

Подробное обоснование:

Для реализации reverseString(str):
T(n) = T(n-1) + O(n)
T(0) = O(1)
T(1) = O(1)

Где:
- T(n-1) - рекурсивный вызов для подстроки длины n-1
- O(n) - операция substring/slicing (создание новой строки)

Решение рекуррентного соотношения:
T(n) = T(n-1) + n
     = T(n-2) + (n-1) + n
     = T(n-3) + (n-2) + (n-1) + n
     = ...
     = 1 + 2 + 3 + ... + n
     = n(n+1)/2 ∈ O(n²)

Для эффективной реализации reverseStringEfficient(str, left, right):
T(n) = T(n-2) + O(1)
T(0) = O(1)
T(1) = O(1)

Решение: T(n) = O(n)

Пространственная сложность: O(n)

· Стек рекурсивных вызовов: O(n)
· Память для хранения строк: O(n)

🚀 Запуск и использование

Компиляция и выполнение:

Java:
javac StringReverse.java
java StringReverse

C++:
g++ -o string_reverse string_reverse.cpp
./string_reverse

Python:
python string_reverse.py

Пример вывода:
=== Рекурсивный реверс строки ===
Исходная: "hello" -> Перевернутая: "olleh"
Исходная: "algorithm" -> Перевернутая: "mhtirogla"
Исходная: "recursion" -> Перевернутая: "noisrucer"
Исходная: "a" -> Перевернутая: "a"
Исходная: "" -> Перевернутая: ""

=== Анализ рекурсии для 'hello' ===
Вызов: reverse_string('hello')
Рекурсивный вызов: 'o' + reverse_string('hell')
  Вызов: reverse_string('hell')
  Рекурсивный вызов: 'l' + reverse_string('hel')
    Вызов: reverse_string('hel')
    Рекурсивный вызов: 'l' + reverse_string('he')
      Вызов: reverse_string('he')
      Рекурсивный вызов: 'e' + reverse_string('h')
        Вызов: reverse_string('h')
        Базовый случай: возвращаем 'h'
      Возврат: 'e' + результат от 'h' = 'eh'
    Возврат: 'l' + результат от 'he' = 'leh'
  Возврат: 'l' + результат от 'hel' = 'lleh'
Возврат: 'o' + результат от 'hell' = 'olleh'

📊 Сравнительный анализ реализаций

Характеристика Базовая реализация Эффективная реализация
Временная сложность O(n²) O(n)
Пространственная сложность O(n) O(n)
Простота понимания ⭐ Высокая Средняя
Эффективность Низкая ⭐ Высокая

Рекомендации:

· Для обучения: Базовая реализация - лучше демонстрирует принципы рекурсии
· Для production: Эффективная реализация - лучше производительность
· Для больших строк: Всегда использовать эффективную реализацию

Контрольный вопрос: Какова временная сложность задачи о N ферзях?

Ответ: Временная сложность задачи о N ферзях составляет O(N!)

---
Контрольный вопрос

📊 Подробное обоснование

1. Алгоритмический анализ

Задача решается методом backtracking (поиск с возвратом):
def solve_n_queens(row, n, queens):
    if row == n:  # Базовый случай
        save_solution()
        return
    
    for col in range(n):  # N вариантов в каждой строке
        if is_safe(row, col):  # O(N) проверок
            place_queen(row, col)
            solve_n_queens(row + 1, n)  # Рекурсивный вызов
            remove_queen(row, col)      # Backtrack

2. Рекурсивное дерево вызовов
Уровень 0: N выборов (для 1-й строки)
Уровень 1: ≤ N-1 выборов (исключаем конфликтные столбцы)  
Уровень 2: ≤ N-2 выборов
...
Уровень N-1: 1 выбор

Максимальное количество листьев: N × (N-1) × (N-2) × ... × 1 = N!

3. Рекуррентное соотношение
T(N) = N × [O(N) + T(N-1)]
T(0) = O(1)

Где:
- N: количество вариантов в текущей строке
- O(N): время проверки безопасности
- T(N-1): рекурсивный вызов для оставшихся строк

4. Решение рекуррентного соотношения
T(N) = N × [N + T(N-1)]
     = N² + N × T(N-1)
     = N² + N × [(N-1)² + (N-1) × T(N-2)]
     = N² + N×(N-1)² + N×(N-1)×T(N-2)
     = ...
     = N² + N×(N-1)² + N×(N-1)×(N-2)² + ... + N! × T(0)

Доминирующий член: O(N! × N) ≈ O(N!)

---

🔍 Детальный разбор факторов сложности

Фактор 1: Количество расстановок

· Теоретический максимум: N! перестановок столбцов
· Фактически: Меньше из-за раннего отсечения диагональных конфликтов
· Но в худшем случае: O(N!)

Фактор 2: Проверка безопасности
def is_safe(row, col):
    for i in range(row):  # O(N) операций
        if queens[i] == col:              # Конфликт по вертикали
            return False
        if abs(queens[i] - col) == abs(i - row):  # Конфликт по диагонали
            return False
    return True

Фактор 3: Эффективность backtracking

· Худший случай: O(N!) - перебор всех перестановок
· Средний случай: Лучше благодаря раннему отсечению
· Но асимптотика остается: O(N!)

---

📈 Практические значения для различных N

N Количество решений Примерное время выполнения O(N!)
4 2 < 1ms 24
8 92 ~10ms 40,320
10 724 ~100ms 3.6M
12 14,200 ~5s 479M
15 2,279,184 ~15 минут 1.3T

---

🎯 Сравнение с альтернативными подходами

1. Наивный перебор: O(N^N)
# Перебор всех возможных расстановок
for each possible arrangement of N queens:
    if valid: save_solution

N=8: 8⁸ = 16,777,216 vs 8! = 40,320

2. Backtracking: O(N!)
# Раннее отсечение невалидных расстановок
if not is_safe: return  # Экономит время

3. Оптимизированные алгоритмы: O(N!)

· Симметрии и эвристики уменьшают константу
· Но асимптотика остается O(N!)

---

⚡ Почему нельзя сделать лучше?

Комбинаторная природа задачи:

· Каждое решение - уникальная перестановка
· N ферзей → N! возможных расстановок столбцов
· Даже с отсечениями нужно проверить экспоненциальное число вариантов

NP-полная задача:

· Не существует известного полиномиального алгоритма
· O(N!) - характерно для задач полного перебора

---

💡 Вывод

Временная сложность задачи о N ферзях: O(N!)

Ключевые факторы:

1. Комбинаторный взрыв: N! возможных расстановок
2. Рекурсивный backtracking: O(N) операций на каждом уровне
3. NP-полнота: Не существует эффективного полиномиального решения
4. Практические ограничения: Алгоритм применим только для N ≤ 15-20

Эта сложность делает задачу о N ферзях классическим примером комбинаторного взрыва и демонстрирует ограничения полного перебора для больших значений N.
